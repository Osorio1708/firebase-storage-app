specVersion: "v1"
serviceId: "firebase-storage-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebase-storage-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
